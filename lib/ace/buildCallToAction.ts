export function buildCallToAction(platform: string) {
  const p = `Şimdi yukarıdaki alana bir ${platform} video bağlantısı yapıştır, formatını seç ve indir butonuna tıkla. Fastdowner, saniyeler içinde dosyanı hazırlasın; sen de içeriğini dilediğin zaman, dilediğin yerde keyifle kullan.`;
  return p;
}
